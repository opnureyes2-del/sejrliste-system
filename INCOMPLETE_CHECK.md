# 🚨 INCOMPLETE CHECK - LÆS DETTE FØRST!

> **DETTE VISES AUTOMATISK VED SESSION START**
> Hvis denne fil har indhold = noget mangler!

---

## ⚠️ AKTIVE MANGLER

<!-- AUTO-GENERATED by auto_verify.py - DO NOT EDIT MANUALLY -->

**Sidst opdateret:** Kør `python3 scripts/auto_verify.py --check-incomplete`

### Sådan Tjekker Du Selv:

```bash
cd "/home/rasmus/Desktop/sejrliste systemet"

# Se hvad der mangler:
python3 scripts/auto_verify.py --check-incomplete

# Eller manuelt:
grep -r "\- \[ \]" 10_ACTIVE/*/SEJR_LISTE.md
```

---

## 🔒 ANTI-TOMME-ORD SYSTEM

### Regel 1: Checkboxes Kræver Bevis
```markdown
❌ FORBUDT:
- [x] Task done

✅ PÅKRÆVET:
- [x] Task done
  - Verify: `kommando der beviser det`
  - Result: Faktisk output
```

### Regel 2: Scores Kræver Begrundelse
```markdown
❌ FORBUDT:
### PASS 1 SCORE: 7/10

✅ PÅKRÆVET:
### PASS 1 SCORE: 7/10
**Begrundelse:**
- ✅ X opnået
- ⚠️ Y mangler
```

### Regel 3: "Færdig" Kræver 300% FÆRDIGT
```
100% RUNNING: Det kører
200% PROVEN: Det virker med real data
300% TESTED: 5+ uafhængige tests
```

---

## 📋 QUICK CHECK KOMMANDOER

```bash
# 1. Hvad mangler lige nu?
grep -r "\- \[ \]" 10_ACTIVE/ | head -20

# 2. Hvor mange checkboxes er done vs pending?
echo "Done:"; grep -r "\- \[x\]" 10_ACTIVE/ | wc -l
echo "Pending:"; grep -r "\- \[ \]" 10_ACTIVE/ | wc -l

# 3. Kan nogen sejr arkiveres?
python3 scripts/auto_verify.py --all

# 4. Hvad er min score?
python3 scripts/admiral_tracker.py --leaderboard
```

---

## 🛡️ PERMANENT BESKYTTELSE

Dette system TVINGER verifikation:

1. **CLAUDE.md** i hver sejr = fokus lock
2. **STATUS.yaml** = single source of truth
3. **auto_verify.py** = checker ALTID 3-pass krav
4. **auto_archive.py** = BLOKERER arkivering uden bevis
5. **INCOMPLETE_CHECK.md** = denne fil viser mangler FØRST

**Du kan IKKE snyde systemet.**

---

## 🎯 HVAD NU?

1. Kør `python3 scripts/auto_verify.py --all`
2. Find første incomplete checkbox
3. GØR DEN
4. Gentag

---

**Sidst opdateret:** 2026-01-26
