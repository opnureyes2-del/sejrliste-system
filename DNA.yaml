# SEJR LISTE SYSTEM - DNA
# System selvbevidsthed og metadata

meta:
  created: "2026-01-23T06:30:00+01:00"
  created_by: "Kv1nt (Claude Sonnet 4.5)"
  version: "1.0.0"
  purpose: "Automated victory list tracking system with 7 DNA layers"

system_identity:
  name: "Sejr Liste System"
  type: "Template & Tracking System"
  location: "/home/rasmus/Desktop/sejrliste systemet"
  standalone: true  # Not part of MASTER FOLDERS or INTRO

dna_layers:
  1_self_aware:
    enabled: true
    description: "System knows its own state via this DNA.yaml"
    implementation: "DNA.yaml with metadata + context"

  2_self_documenting:
    enabled: true
    description: "Auto-logs all actions inline (not retrospective)"
    implementation: "AUTO_LOG.jsonl in each active sejr"

  3_self_verifying:
    enabled: true
    description: "Auto-checks task completion"
    implementation: "auto_verify.py + VERIFY_STATUS.yaml"

  4_self_improving:
    enabled: true
    description: "Learns patterns from completed sejr"
    implementation: "auto_learn.py + PATTERNS.yaml"

  5_self_archiving:
    enabled: true
    description: "Auto-cleans up when done (semantic only)"
    implementation: "auto_archive.py + 90_ARCHIVE/"

  6_predictive:
    enabled: true
    description: "AI-suggests optimal next steps"
    implementation: "auto_predict.py + NEXT.md generation"

  7_self_optimizing:
    enabled: true
    description: "MANDATORY creative search before building"
    implementation: "External search + Internal search + 3 alternatives"

context:
  what_before: "Manual task tracking in scattered TODO files"
  what_now: "Automated 7-layer DNA system with semantic focus"
  what_next: "Use for all project tracking (HYBRID deploy, etc.)"
  why: "Prevent 'NÆSTEN' pattern, ensure 300% FÆRDIGT standard"

status:
  operational: true
  last_updated: "2026-01-23T22:55:27+01:00"
  active_sejr_count: 1  # Updated by scripts
  archived_sejr_count: 0  # Updated by scripts

integration:
  todowrite:
    enabled: true
    sync_direction: "bi-directional"
    description: "SEJR_LISTE ↔ TodoWrite (two-way sync)"

  git:
    enabled: true
    auto_commit: true
    auto_push: true
    trigger: "on sejr complete"

  context_system:
    enabled: true
    hooks:
      - "journal.md (auto-update on completion)"
      - "session.md (auto-update current state)"
      - "cleanup old context"

quality_standards:
  semantic_focus: "Max 500 lines context, always current"
  automation_level: "90% auto, 10% human decisions"
  token_efficiency: "<1000 tokens per session resume"
  visual_clarity: "Progress visible in <5 seconds"
  prevention_score: "Blocks 80% of repeat mistakes"

admiral_compliance:
  rule_16_verification: true  # NEVER FORGET VERIFICATION + DOCUMENTATION
  rule_28_git_complete: true  # Work complete = Git complete (all 5 steps)
  rule_0c_300_faerdig: true   # RUNNING + PROVEN + TESTED
  foer_under_efter: true      # Always document phases

documentation:
  master_folders: "/home/rasmus/Desktop/MASTER FOLDERS(INTRO)/I12_SEJR_LISTE_SYSTEM.md"
  hjerneskade: "/home/rasmus/Desktop/MANUAL I TILFÆLDE AF HJERNESKADE/26_SEJR_LISTE_SYSTEM.md"
  readme: "/home/rasmus/Desktop/sejrliste systemet/README.md"

verification:
  system_exists: "ls '/home/rasmus/Desktop/sejrliste systemet'"
  readme_exists: "cat '/home/rasmus/Desktop/sejrliste systemet/README.md'"
  scripts_count: "ls '/home/rasmus/Desktop/sejrliste systemet/scripts/'*.py | wc -l  # Expected: 6"
  templates_count: "ls '/home/rasmus/Desktop/sejrliste systemet/00_TEMPLATES/'*.md | wc -l  # Expected: 2+"
  state_exists: "cat '/home/rasmus/Desktop/sejrliste systemet/_CURRENT/STATE.md'"
